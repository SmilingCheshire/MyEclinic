<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center_horizontal">

        <!-- ListView to Display Specializations (Expands to Fill Space) -->
        <ListView
            android:id="@+id/list_specializations"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="vertical"
            android:padding="8dp"
            android:layout_gravity="center" />

        <!-- Bottom Container for Input and Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="16dp"
            android:gravity="bottom">

            <!-- EditText for Entering Specialization -->
            <EditText
                android:id="@+id/et_specialization"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Enter specialization"
                android:minHeight="48dp"
                android:layout_marginBottom="8dp" />

            <!-- Add Specialization Button -->
            <Button
                android:id="@+id/btn_add_specialization"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Add Specialization"
                android:layout_marginBottom="8dp" />

            <!-- Remove Specialization Button -->
            <Button
                android:id="@+id/btn_remove_specialization"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Remove Specialization"
                android:backgroundTint="@android:color/holo_red_light"
                android:textColor="@android:color/white" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>
